# פרומפטים לקלוד בכרום

כל פעולה שאני לא יכול לבצע מכאן, עם פרומפט מוכן להעתקה.
מסודר לפי מה שחוסם אותך עכשיו.

**לפני שמתחילים — שלוש אמיתות על קלוד בכרום:**

* הוא רץ בכרום **במחשב**. לא בטלפון. כל מה שדורש את הטלפון נשאר עליך.
* הוא **לא יכול ללחוץ על חלוניות ההרשאה של הדפדפן עצמו** ("האתר מבקש
  לשלוח התראות", "לאפשר מיקרופון", בחירת תיקייה). הוא יביא אותך לכפתור
  הנכון; הלחיצה על החלונית הכחולה היא שלך. זו לחיצה אחת.
* **אל תדביק סודות בצ'אט.** כשפרומפט מבקש טוקן, הוא מבקש להעביר אותו
  *בתוך הדפדפן* מדף לדף — לא להקריא אותו בשיחה.

בכל פרומפט: **תעצור אם משהו לא נראה כמו שתיארתי, ותשאל.** זה מכוון.

---

## 1 · ליצור `main` בגיטהאב, כדי שיהיה PR

**למה זה חסום:** הענף `claude/front-management-system-apy8j0` הוא גם ענף
ברירת המחדל של הריפו, אז אין ענף בסיס למזג אליו. אני לא דוחף לענף אחר
בלי רשות מפורשת.

**הדרך הקצרה:** תגיד לי "צור main" ואני אעשה את זה מכאן בשנייה. הפרומפט
למטה הוא אם אתה מעדיף שזה יקרה בממשק.

```
היכנס ל-https://github.com/alongerber/front

אני רוצה להפוך את המצב הנוכחי לענף ברירת מחדל בשם main, כדי שאפשר
יהיה לפתוח Pull Request מענף הפיתוח.

1. בדוק אילו ענפים קיימים ומה ענף ברירת המחדל כרגע.
2. צור ענף חדש בשם main מתוך הענף הנוכחי
   (claude/front-management-system-apy8j0).
3. ב-Settings → Branches, שנה את ענף ברירת המחדל ל-main.
4. אל תמחק שום ענף. אל תשנה שום הגדרה אחרת.
5. דווח לי: אילו ענפים קיימים עכשיו, ומה ברירת המחדל.

אם ענף בשם main כבר קיים — תעצור, תגיד לי מה יש בו, ואל תיגע בו.
```

---

## 2 · Vercel — מקצה לקצה, עד שזה עובד

**המצב:** הפרויקט `front` מפורסם, אבל הפרסום האחרון הוא מלפני שלושה
ימים — הסנכרון, רעיון מהיר, ההשהיה בטיימר וכל תיקוני הנייד **לא
באוויר**. הפרומפט הזה מביא הכל ומדליק את הסנכרון, ולא נעצר עד
שהבדיקה בסוף עוברת.

**מה להכין לפני:** פתח לשונית עם המערכת ולשונית עם Vercel. זהו.

```
אתה עובד מול חשבון Vercel שלי, פרויקט בשם front, בארגון alon's projects.
המטרה: שהמערכת תהיה מעודכנת באוויר ושהסנכרון בין המחשב לטלפון יעבוד.

כללי עבודה:
· תעבוד לפי הסדר. אחרי כל שלב תגיד לי מה אתה רואה, בקצרה.
· אם מסך לא נראה כמו שתיארתי — תעצור ותשאל. אל תנחש ואל תמציא
  שמות של תפריטים.
· סודות: כשאתה מגיע לשדה Value של משתנה סביבה, תעצור ותגיד לי
  להדביק בעצמי. אל תבקש ממני להקריא, אל תכתוב אותם בשיחה, ואל
  תצלם אותם.
· אל תמחק שום משתנה קיים ואל תשנה הגדרות שלא ביקשתי.

────────────────────────────────
שלב 1 — לוודא שהקוד העדכני יגיע
   Vercel → הפרויקט front → Settings → Git.
   תגיד לי לאיזה ענף (Production Branch) הפרויקט מחובר.
   הקוד שאני רוצה יושב בענף claude/front-management-system-apy8j0.
   · אם זה הענף המחובר — מצוין, תמשיך.
   · אם זה ענף אחר — תעצור ותגיד לי מה השם, אני אחליט.

שלב 2 — אחסון לסנכרון
   Storage (בתפריט הימני של הפרויקט, או בסרגל העליון של החשבון) →
   Create Database → בחר Blob → תן לו שם כלשהו → צור →
   ואז חבר אותו לפרויקט front (Connect Project).
   שים לב: בממשק החדש Vercel מחבר דרך OIDC ומוסיף BLOB_STORE_ID
   ו-BLOB_WEBHOOK_PUBLIC_KEY, ולא בהכרח BLOB_READ_WRITE_TOKEN.
   זה בסדר בשלב הזה — בדיקת החיבור בסוף תגלה אם הכתיבה עובדת
   בלעדיו. תרשום לי אילו משתנים נוספו, בשמות בלבד.
   אל תעתיק אף ערך, ואל תלחץ על "Revoke Token".

שלב 3 — הטוקן של הסנכרון
   תגיד לי לעבור ללשונית של המערכת → הגדרות → הכרטיס
   "סנכרון בין מחשב לטלפון" → ללחוץ "🔑 צור טוקן חדש".
   הוא נשמר בדפדפן ומועתק ללוח שלי. תחכה שאגיד לך שהוא בלוח.

שלב 4 — משתני סביבה
   Settings → Environment Variables. הוסף:
   · Key: SYNC_TOKEN
     Value: תעצור, אני מדביק מהלוח.
     Environments: לסמן את שלושתם — Production, Preview, Development.
   ואז, אם אני אומר שאני רוצה גם את העוזר:
   · Key: ANTHROPIC_API_KEY
     Value: תעצור, אני מביא אותו מ-
     https://console.anthropic.com/settings/keys
     Environments: שלושתם.
   תאשר לי איזה משתנים קיימים עכשיו, בשמות בלבד ובלי ערכים.

שלב 5 — פרסום מחדש
   Deployments → הפרסום העליון → תפריט ⋯ → Redeploy.
   אל תסמן "Use existing Build Cache".
   תחכה שהוא Ready ותגיד לי את הכתובת המלאה.
   · אם הוא נכשל — פתח את Deploy Logs, תקרא לי את השגיאה
     במדויק, ותעצור. אל תנסה לתקן קוד.

────────────────────────────────
שלב 6 — הבדיקה. כאן זה מוכרע.
   פתח את כתובת האתר מהשלב הקודם ועבור על ארבע הבדיקות.
   אחרי כל אחת תגיד לי עובר או נכשל.

   6א. הקוד החדש באוויר
       יש בפס העליון כפתור "🪟 חלון צף" וכפתור "⏸ השהה"?
       · אין → הפרסום לא כלל את הקוד החדש. חזרה לשלב 1.

   6ב. הסנכרון מחובר
       הגדרות → סנכרון → "בדוק חיבור". תקרא לי בדיוק מה כתוב:
       · "מחובר" → עובר, תמשיך.
       · משהו על SYNC_TOKEN → המשתנה חסר, או שלא עשינו Redeploy
         אחריו. חזרה לשלב 4 ואז 5.
       · "כתיבה לאחסון נכשלה" → הקריאה עובדת אבל הכתיבה לא. זה
         בדיוק המצב שבו חיבור ה-OIDC לא מספיק: Storage → ה-Store
         → תפריט החיבור → ליצור BLOB_READ_WRITE_TOKEN, ואז
         לחזור לשלב 5 ולפרסם מחדש.
       · "קריאה מהאחסון נכשלה" או "האחסון לא זמין" → האחסון לא
         מחובר בכלל. חזרה לשלב 2.
       · "הטוקן כאן לא תואם" → הטוקן במערכת שונה מזה שב-Vercel.
         תגיד לי, אני אסדר.

   6ג. הסנכרון באמת מעביר
       סמן "סנכרון פעיל" → "↻ סנכרן עכשיו" → תקרא לי את שורת המצב.
       ואז: פתח את אותה כתובת בחלון גלישה בסתר, הדבק שם את אותו
       טוקן, הדלק סנכרון, ולחץ "סנכרן עכשיו".
       תגיד לי אם הפריטים מהחלון הרגיל מופיעים בחלון הסתר.
       · לא מופיעים → תקרא לי מה כתוב בשורת המצב בשני החלונות.

   6ד. אין שגיאות
       פתח את קונסולת הדפדפן (F12 → Console), רענן, ותקרא לי כל
       שורה אדומה. שורות על גופנים של גוגל — להתעלם.

────────────────────────────────
בסוף תן לי סיכום קצר: הכתובת, אילו משתנים קיימים (שמות בלבד),
ואיזו מארבע הבדיקות עברה ואיזו לא. אם משהו נכשל — תגיד מה בדיוק
ראית, אל תנסה לעקוף.
```

**אחרי שזה עובד — הצעד היחיד שנשאר עליך:** בטלפון, לפתוח את אותה
כתובת → הגדרות → סנכרון → להדביק את אותו טוקן → להדליק. ואז
שיתוף ← "הוסף למסך הבית".

## 3 · שלוש ההרשאות ושתי ההתקנות

**למה זה חסום:** חלוניות ההרשאה של הדפדפן הן מחוץ להישג יד של כל
תוסף, כולל קלוד בכרום. הוא יביא אותך לכפתור; הלחיצה שלך.

הפרומפט הזה שווה בעיקר בשביל **שלא תשכח אחד מהם** ובשביל שהוא יבדוק
בסוף שהכל אכן פעיל.

```
פתח את מערכת פרונט (הכתובת מ-Vercel) ולחץ על הכפתור ? בפס
העליון. זה פותח עמוד מדריך עם רשימה בשם "מה עוד לא הגדרת".

עבור על הרשימה מלמעלה למטה ולכל שורה שלא מסומנת ✓, לחץ על הכפתור
שלה והוביל אותי עד הסוף:

· "לאשר זיהוי נוכחות" ו"לאשר התראות" — יקפצו חלוניות הרשאה של כרום.
  אתה לא יכול ללחוץ עליהן. תגיד לי בדיוק מה מופיע ומה ללחוץ,
  ותחכה שאאשר.
· "לבחור תיקיית גיבוי" — ייפתח חלון בחירת תיקייה של המערכת. גם עליו
  אתה לא יכול ללחוץ. תגיד לי לבחור תיקייה שמסונכרנת לדרייב.
· "להתקין על מסך הבית" — במחשב זה תפריט ⋮ ← "התקן אפליקציה".
· "להכניס לקוח אחד" — תעביר אותי לעמוד הצינור ותגיד לי מה למלא.

בסוף: רענן את העמוד, ותאשר לי שהרשימה מראה "✓ הכל מוגדר". אם משהו
נשאר לא מסומן — תגיד לי מה, ולמה לדעתך.
```

**מה שנשאר רק עליך, בלי עזרה:** אותו דבר **בטלפון**. פתח שם את
המערכת, שיתוף ← "הוסף למסך הבית" (או ⋮ ← "התקן אפליקציה"), ואשר
מיקרופון בפעם הראשונה שתלחץ "הכתב". קלוד בכרום לא רץ בטלפון.

---

## 4 · Meta Ads — הוצאת מודעות ועלות לליד אמיתיות

**למה זה חסום:** דורש חשבון Business Manager שלך, אפליקציית Meta,
ואימות עסק. אף אחד לא יכול לעשות את זה במקומך מבחינת הזהות; מה שכן
אפשר זה שקלוד בכרום ינווט אותך בכל המסכים ויאסוף את המזהים.

זה החלק הארוך. **תריץ אותו כשיש לך שלושת רבעי שעה פנויה**, לא בין
הדברים.

```
אני רוצה למשוך את הוצאת המודעות והלידים שלי מ-Meta לאפליקציה פרטית
שלי. אני צריך שלושה דברים: מזהה חשבון המודעות, טוקן שלא פג, ורשימת
טפסי הלידים.

הובל אותי צעד צעד. אחרי כל צעד תגיד לי מה בדיוק לראות על המסך, ואם
מה שאני רואה לא תואם — תעצור ותשאל, אל תנחש.

צעד 1 — מזהה חשבון המודעות
   https://adsmanager.facebook.com → מצא את מזהה החשבון (act_ ואחריו
   מספרים). תרשום אותו לי.

צעד 2 — אימות עסק
   https://business.facebook.com/settings → Business Info → בדוק אם
   העסק מאומת (Verified). אם לא — תסביר לי אילו מסמכים צריך ומה
   התהליך, אבל אל תתחיל אותו בשמי.

צעד 3 — אפליקציה
   https://developers.facebook.com/apps → צור אפליקציה מסוג Business
   בשם "Front Internal". תרשום לי את ה-App ID.

צעד 4 — משתמש מערכת עם טוקן שלא פג
   business.facebook.com/settings → Users → System Users → Add.
   · שם: Front Sync
   · תפקיד: Employee
   · Assign Assets → חשבון המודעות מצעד 1 → הרשאת View Performance
   · Generate New Token → בחר את האפליקציה מצעד 3
   · סמן את ההרשאות: ads_read, leads_retrieval
   כשהטוקן מוצג — תגיד לי להעתיק אותו בעצמי ולשמור אותו במקום מוגן.
   אל תכתוב אותו בשיחה ואל תבקש ממני להקריא אותו.

צעד 5 — טפסי לידים
   business.facebook.com → Instant Forms → תרשום לי את השם והמזהה של
   כל טופס פעיל.

בסוף תן לי סיכום מרוכז: מזהה חשבון, App ID, שמות ומזהי הטפסים, ומה
מתוך ההרשאות דורש App Review לפני שזה יעבוד בייצור.
```

**כשתחזור עם הסיכום הזה** — תדביק לי אותו כאן (בלי הטוקן) ואני כותב
את הקוד שמושך את המספרים.

---

## 5 · יומן גוגל — פגישות שנרשמות לבד

**למה זה חסום:** הכתובת הפרטית של הלוח היא סוד בחשבון שלך.

זו האינטגרציה הזולה ביותר: בלי OAuth, בלי אפליקציה, בלי אימות.

```
היכנס ל-https://calendar.google.com/calendar/r/settings

אני צריך את הכתובת הפרטית ב-iCal של הלוח הראשי שלי, כדי שאפליקציה
פרטית שלי תוכל לקרוא ממנו פגישות (קריאה בלבד).

1. בתפריט הימני, בחר את הלוח הראשי שלי → Integrate calendar.
2. גלול ל-"Secret address in iCal format".
3. תגיד לי להעתיק אותה בעצמי. אל תכתוב אותה בשיחה — מי שמחזיק
   בכתובת הזאת רואה את כל היומן שלי.
4. תסביר לי איפה בדף הזה מאפסים אותה אם היא נחשפת, כדי שאדע.
```

---

## 6 · לאשר את המחבר של Meta בקלוד

**למה זה חסום:** אני לא יכול להריץ התחברות OAuth מהסשן הזה.

יש בסביבה הזאת מחבר ל-Meta Ads שלא מאושר, ולכן אני לא יכול לשלוף את
מספרי הקמפיינים שלך בשיחה איתי. זה **נפרד לגמרי** מהאפליקציה שלך —
זה רק בשביל שאני אוכל להסתכל על הנתונים כשאנחנו עובדים.

זה לא פרומפט אלא שני קליקים: claude.ai ← הגדרות ← Connectors ←
למצוא את Meta Ads ← Connect.

---

## 7 · להדליק את הסנכרון

**למה זה כמעט לא חסום:** הקוד נבנה. אם עשית את מספר 2 — האחסון והטוקן
כבר במקום, ונשאר מתג אחד בכל מכשיר.

זה מה שמחליף את כל הריקוד של ייצוא־שליחה־ייבוא: מה שקלטת בטלפון מופיע
במחשב לבד, תוך שניות, כולל מחיקות.

```
פתח את מערכת פרונט → הגדרות → הכרטיס "סנכרון בין מחשב לטלפון".

1. אם השדה "טוקן הסנכרון" ריק — לחץ "🔑 צור טוקן חדש". הוא נשמר
   ומועתק ללוח, וצריך להיות זהה ל-SYNC_TOKEN שב-Vercel.
2. לחץ "בדוק חיבור" ותקרא לי בדיוק מה כתוב:
   · "מחובר" — תמשיך לצעד 3.
   · משהו על SYNC_TOKEN — המשתנה לא הוגדר ב-Vercel, או שלא עשיתי
     Redeploy אחרי שהוספתי אותו. תגיד לי מה מהשניים.
   · משהו על Vercel Blob — לא חיברתי אחסון. חזרה לשלב 1 של פרומפט 2.
   · "הטוקן כאן לא תואם" — יש טוקן שונה בשני הצדדים. תעצור.
3. סמן "סנכרון פעיל", ואז "↻ סנכרן עכשיו".
4. תקרא לי את שורת המצב (כמה חדשים, כמה עודכנו).

אל תלחץ "צור טוקן חדש" אם כבר יש טוקן בשדה — זה מחליף אותו, המכשיר
השני יפסיק להסתנכרן, ומה שכבר נשמר בענן יהפוך ללא-קריא עד שאעדכן
את SYNC_TOKEN גם ב-Vercel.
```

**מה שנשאר רק עליך:** אותו דבר **בטלפון** — הגדרות → סנכרון → להדביק
את אותו טוקן → להדליק. פעם אחת.

## 8 · הסיבוב השני — לסגור את הסנכרון

**מתי להשתמש בזה:** אחרי שכבר עשית את סעיף 2 (אחסון + משתנים +
פרסום). הפרומפט הזה מביא את הקוד העדכני, בודק שהכתיבה לאחסון באמת
עובדת, ומדליק את הסנכרון בשני המכשירים.

**למה זה נחוץ:** בדיקת החיבור הקודמת קראה בלבד. אחסון ריק מחזיר
"אין כלום" בהצלחה, ולכן היא ענתה "מחובר" בלי שהכתיבה נוסתה אפילו
פעם אחת. הבדיקה המתוקנת כותבת בפועל — והיא קיימת רק בקומיט
`bf73f5a` ואילך.

```
אתה עובד מול חשבון Vercel שלי, פרויקט front, ומול המערכת עצמה
בכתובת front-lovat-seven.vercel.app.

המצב: האחסון (Blob) מחובר, SYNC_TOKEN ו-ANTHROPIC_API_KEY קיימים,
והפרויקט פורסם. נשארו שלושה דברים: להביא את הקוד העדכני, לוודא
שהכתיבה לאחסון עובדת, ולהדליק את הסנכרון.

כללי עבודה:
· תעבוד לפי הסדר, ותגיד לי בקצרה מה אתה רואה אחרי כל שלב.
· אם מסך לא נראה כמו שתיארתי — תעצור ותשאל. אל תנחש.
· אל תעתיק ואל תציג ערכים של משתני סביבה או טוקנים.
· אל תלחץ על "Revoke Token" בשום שלב.

────────────────────────────────
שלב 1 — להביא את הקוד העדכני
   Vercel → front → Deployments.
   הקומיט שאני צריך הוא bf73f5a.
   · אם השורה העליונה כבר bf73f5a — תגיד לי, ותמשיך לשלב 2.
   · אם השורה העליונה היא קומיט ישן יותר — חפש ברשימה פרסום של
     bf73f5a. אם הוא שם, פתח אותו ועשה לו Promote to Production.
     אם הוא לא שם בכלל, תעצור ותגיד לי — סימן שהחיבור לגיט לא
     מפרסם אוטומטית.
   · בכל מקרה, בסוף: Redeploy, בלי לסמן "Use existing Build Cache".
   תחכה ל-Ready ותגיד לי.

שלב 2 — לתקן את הגדרת SYNC_TOKEN
   Settings → Environment Variables → SYNC_TOKEN → Edit.
   אני מעדיף שהוא יהיה Sensitive. Development לא נחוץ לי.
   · הורד את הסימון מ-Development, והדלק את המתג Sensitive.
   · השאר אותו מסומן ב-Production וב-Preview.
   · אל תשנה את הערך ואל תיצור טוקן חדש.
   אם Vercel לא נותן לערוך ורק לחשוף מחדש — תעצור ותגיד לי,
   כי החלפת הערך תדרוש עדכון גם באפליקציה.

שלב 3 — הבדיקה שקובעת
   פתח את front-lovat-seven.vercel.app → הגדרות → הכרטיס
   "סנכרון בין מחשב לטלפון" → לחץ "בדוק חיבור".
   תקרא לי את ההודעה מילה במילה, ותפעל לפיה:

   · "מחובר, קריאה וכתיבה עובדות" → מצוין. לשלב 4.

   · "כתיבה לאחסון נכשלה" → זה הכשל שחיפשנו. תקן אותו:
       Storage → ה-Store בשם front-blob → תפריט החיבור לפרויקט →
       ליצור BLOB_READ_WRITE_TOKEN → לוודא שהוא מסומן ב-Production
       וב-Preview → ואז Deployments → Redeploy בלי build cache.
       אחרי שזה Ready — לחזור על "בדוק חיבור" ולקרוא לי שוב.

   · "קריאה מהאחסון נכשלה" או "האחסון לא זמין" → האחסון לא באמת
     מחובר לפרויקט. תעצור ותגיד לי מה מופיע בדף ה-Store.

   · משהו על SYNC_TOKEN → המשתנה חסר או שהפרסום לא כלל אותו.
     תחזור לשלב 1 ותפרסם שוב.

   · "הטוקן כאן לא תואם" → הערך באפליקציה שונה מזה שב-Vercel.
     תעצור ותגיד לי.

שלב 4 — להדליק
   באותו כרטיס: לסמן "סנכרון פעיל", ואז ללחוץ "↻ סנכרן עכשיו".
   תקרא לי את שורת המצב שמופיעה מתחת.

שלב 5 — לוודא שזה באמת מעביר נתונים
   זו הבדיקה האמיתית, לא הסטטוס.
   1. בחלון הרגיל: תכתוב בשורה העליונה של המערכת
      "בדיקת סנכרון 1" ותלחץ הוסף.
   2. תחכה 15 שניות ותלחץ "סנכרן עכשיו".
   3. תפתח חלון גלישה בסתר על אותה כתובת.
      שם: הגדרות → סנכרון → תגיד לי להדביק את הטוקן (אני אביא
      אותו מהחלון הראשון) → לסמן "סנכרון פעיל" → "סנכרן עכשיו".
   4. תגיד לי אם "בדיקת סנכרון 1" מופיע בחלון הסתר.
      · מופיע → הסנכרון עובד. תמחק אותו ותסיים.
      · לא מופיע → תקרא לי את שורת המצב בשני החלונות.

────────────────────────────────
בסוף תן לי סיכום: מה הקומיט שרץ בפרודקשן, מה בדיוק אמרה בדיקת
החיבור, והאם הפריט עבר בין שני החלונות.
```

**מה שנשאר רק עליך:** בטלפון — לפתוח את הכתובת, הגדרות → סנכרון,
להדביק את אותו טוקן ולהדליק. פעם אחת, ואחר כך זה קורה לבד.

## הסדר שאני ממליץ

| # | מה | כמה זמן | מה זה פותח |
|---|---|---|---|
| 1 | `main` בגיטהאב | דקה (או תגיד לי ואני אעשה) | PR |
| 2 | Vercel: אחסון + 2 משתנים + Redeploy | 15 דק' | המערכת המעודכנת באוויר, וסנכרון מוכן |
| 3 | הרשאות והתקנות | 10 דק' | מדידת זמן אמיתית, רעיון מהיר |
| — | **עד כאן המערכת עובדת במלואה** | | |
| 8 | הסיבוב השני — לסגור את הסנכרון | 5 דק' | טלפון ומחשב מדברים לבד |
| 5 | יומן גוגל | 3 דק' | פגישות נרשמות לבד |
| 4 | Meta Ads | 45 דק' | עלות לליד אמיתית |

**אל תתחיל מ-4.** הוא הארוך ביותר והוא לא חוסם כלום.
